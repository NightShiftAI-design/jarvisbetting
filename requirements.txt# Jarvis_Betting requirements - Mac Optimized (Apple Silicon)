
APScheduler==3.10.4
beautifulsoup4==4.12.3
cachetools==5.5.2
httpx==0.28.1
pandas>=2.2.0
numpy>=2.0.0
scikit-learn>=1.5.0
sqlalchemy>=2.0.0
streamlit>=1.38.0
tenacity>=8.0.0
pybaseball>=2.2.0
requests>=2.32.0
python-dotenv>=1.0.0# Jarvis_Betting requirements
# Python target: 3.11+
#
# Notes:
# - Package set is restricted to the libraries explicitly approved in the spec.
# - Versions are pinned where stable, broadly compatible 2026-era releases are known or
#   conservatively constrained for reproducibility.
# - SQLAlchemy, Streamlit, scikit-learn, pandas, and numpy are pinned to a modern 3.11-compatible range.
# - requests and httpx are both included because the system will use:
#   - requests for simple/public API integrations
#   - httpx for timeouts, retries, and async-friendly expansion
#
# Verified package indexes:
# - https://pypi.org/

APScheduler==3.10.4
beautifulsoup4==4.12.3
cachetools==5.5.2
httpx==0.28.1
numpy==2.2.4
pandas==2.2.3
pybaseball==2.2.7
python-dotenv==1.0.1
requests==2.32.3
scikit-learn==1.6.1
SQLAlchemy==2.0.40
streamlit==1.44.1
tenacity==9.0.0
